Search file contents with regex pattern matching

Equivalent to the `grep` command. Searches files for lines matching a regex pattern.
Supports recursive directory search, line numbers, and case-insensitive matching.
