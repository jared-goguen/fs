Edit a file by replacing exact string matches

Performs a safe text replacement in a file. Fails if the oldString isn't found exactly once,
preventing accidental edits. Must match the exact string including whitespace.
